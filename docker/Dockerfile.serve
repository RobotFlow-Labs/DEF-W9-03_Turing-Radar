FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml README.md /app/
COPY src /app/src
COPY configs /app/configs

RUN pip install --no-cache-dir .

EXPOSE 8080
CMD ["python", "-m", "uvicorn", "anima_turing_radar.api:create_app", "--factory", "--host", "0.0.0.0", "--port", "8080"]
