# Task PRD-0101: Package and Config Scaffold

> PRD: PRD-01 | Depends: None
> Files: 4 | Time: ~60m

## What to Build
Create the base package and typed config schema so every downstream component has a stable runtime contract.

## Files
- `pyproject.toml`
- `src/anima_turing_radar/__init__.py`
- `src/anima_turing_radar/constants.py`
- `src/anima_turing_radar/config.py`

## Verify
```bash
python3 -c "from anima_turing_radar.config import load_config; print('ok')"
```

## Done When
- [ ] Package imports cleanly
- [ ] `load_config` handles nested config sections
- [ ] Defaults are documented in code
