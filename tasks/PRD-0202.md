# Task PRD-0202: Embedding Module Hooks

> PRD: PRD-02 | Depends: PRD-0201
> Files: 1 | Time: ~45m

## What to Build
Add optional torch embedding network that can transform PDWs before clustering, with graceful fallback when torch is unavailable.

## Files
- `src/anima_turing_radar/embedding.py`

## Verify
```bash
python3 -c "from anima_turing_radar.embedding import torch_available; print(torch_available())"
```

## Done When
- [ ] Module imports without hard-failing on missing torch
- [ ] Encoder preserves batch dimension
- [ ] Latent dimension is configurable
