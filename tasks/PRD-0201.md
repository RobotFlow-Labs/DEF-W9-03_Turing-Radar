# Task PRD-0201: Sklearn Deinterleaver Interfaces

> PRD: PRD-02 | Depends: PRD-0104
> Files: 1 | Time: ~70m

## What to Build
Implement `Deinterleaver` abstraction and baseline clustering implementations (HDBSCAN/DBSCAN/KMeans/Agglomerative).

## Files
- `src/anima_turing_radar/model.py`

## Verify
```bash
python3 -c "import numpy as np; from anima_turing_radar.model import SklearnClusterDeinterleaver; x=np.random.randn(100,5); print(SklearnClusterDeinterleaver('dbscan').predict(x).shape)"
```

## Done When
- [ ] Algorithms can be selected by config string
- [ ] Unknown algorithm raises `ValueError`
- [ ] Predictions return 1D label vector
